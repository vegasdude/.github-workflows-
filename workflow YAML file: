name: Example Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run script with GitHub Token
        run: echo "Token is available"
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}